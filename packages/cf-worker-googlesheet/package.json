{
  "name": "dpp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:migration": "wrangler dev --config wrangler.migration.toml",
    "deploy": "wrangler deploy",
    "types": "wrangler types --env-interface WorkerBindings worker-configuration.d.ts",
    "check:migration": "tsc --noEmit --project tsconfig.migration.json"
  },
  "dependencies": {
    "google-auth-library": "^10.5.0",
    "google-spreadsheet": "^5.0.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.61.1"
  }
}
